"""Q8. Store the string "Python Programming" in a variable. 
Print its length, its uppercase version, and its lowercase version — each on a separate line."""

string_value = "Python Programming"
print(len(string_value))
print(string_value.upper())
print(string_value.lower())
